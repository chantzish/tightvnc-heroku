build:
  docker:
    web: Dockerfile
  config:
    FOO: |
      [user]
      	email = chantzish@gmail.com
      	name = chantzish
      [credential]
      	helper = store
      [color]
      	ui = auto
