build:
  docker:
    web: Dockerfile
  config:
    GIT_CONFIG: |
      [user]
      	email = chantzish@gmail.com
      	name = chantzish
      [credential]
      	helper = store
      [color]
      	ui = auto
